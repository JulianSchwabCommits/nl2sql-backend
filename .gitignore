node_modules/
dist/
generated/
.env

# Deployment configuration files (may contain sensitive info)
deployment-config.txt
certificate-info.txt

# Local development
*.log
.DS_Store

# Certificates
*.pem
# ...but commit the public RDS CA bundle; without it Docker mounts an empty dir at the path and TLS breaks
!rds-combined-ca-bundle.pem

.env.production